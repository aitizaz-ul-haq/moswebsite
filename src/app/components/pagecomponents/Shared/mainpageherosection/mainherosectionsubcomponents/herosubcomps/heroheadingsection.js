export default function HeroHeadingSection({title}) {
    return(
        <div className="homeright-title-container">
          <h1 className="homeright-title font-poppins">{title}</h1>
        </div>
    );
}